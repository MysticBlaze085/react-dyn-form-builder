import { ServiceAccessPipe } from './service-access.pipe';

describe('ServiceAccessPipe', () => {
  it('create an instance', () => {
    const pipe = new ServiceAccessPipe();
    expect(pipe).toBeTruthy();
  });
});
